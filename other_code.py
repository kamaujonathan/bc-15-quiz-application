"""try:
                enter_name = input("Enter your name: ")
                cmd_check = input("Type Command: ")
            except TypeError:
                print ('That is not a valid input!')"""
            
"""while not (x.isalpha() or x.isspace() for x in enter_name):
                print ('That is not a valid input!')
                try:
                    enter_name = input("Enter your name: ")
                    cmd_check = input("Type Command: ")
                except TypeError:
                    print ('That is not a valid input!')"""
            
"""if re.match ("^([A-Za-z]+)", enter_name):
            cmd_check = input("Type Command: ")
        else:
            print ('That is not a valid input!')
            enter_name = input("Enter your name: ")
            while not re.match ("^([A-Za-z]+)", enter_name):
                print ('That is not a valid input!')
                try:
                    enter_name = input("Enter your name: ")
                    cmd_check = input("Type Command: ")
                except TypeError:
                    print ('That is not a valid input!')"""
 """def check_input():
        enter_name = input("Enter your name: ")
        while not re.match ("[A-Za-z]", answer):
            print ('That is not a valid input!')
            try:
                answer= input("Enter your name: ").upper()
                is_answer(answer)
            except TypeError:
                print ('That is not a valid input!')
        cmd_check = input("Type Command: ")"""

  """path_to_json = "json"        
    json_files = [pos_json for pos_json in os.listdir(path_to_json) if pos_json.endswith('.json')]
    enter_name = input("Enter your name: ")
    first_input = input("Type 'quiz list' to see list of all available quizzes: ")
    initialize_database()
    create_tables()"""
